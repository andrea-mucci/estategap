# Tasks: Kind Cluster & Helm Validation

**Input**: Design documents from `/specs/029-kind-helm-validation/`  
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/makefile-targets.md, quickstart.md

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependency on concurrent tasks)
- **[Story]**: User story label — US1 through US5 (from spec.md)
- All paths relative to repository root `/root/projects/estategap/`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Create directory scaffolding and initialize the fixture project before any implementation begins.

- [X] T001 Create directory tree: `tests/kind/`, `tests/fixtures/listings/`, `tests/fixtures/zones/`, `tests/fixtures/ml-models/`, `tests/fixtures/conversations/`, `tests/fixtures/html-samples/idealista/`, `tests/fixtures/html-samples/seloger/`, `tests/fixtures/html-samples/rightmove/`, `tests/fixtures/html-samples/immobiliare/`, `tests/helm/`, `mk/`, `helm/estategap/tests/`
- [X] T002 Initialize `tests/fixtures/pyproject.toml` as a uv project with dependencies: `asyncpg>=0.29`, `boto3>=1.34`, `redis>=5.0`, `pydantic>=2.0`, `pyyaml>=6.0`, `onnx>=1.16`, `skl2onnx>=1.17`, `scikit-learn>=1.5`
- [X] T003 [P] Add `.make-cache/`, `.kind-pids`, `.buildx-cache/` to `.gitignore`
- [X] T004 [P] Add `tests/fixtures/ml-models/*.onnx` to `.gitignore` and note they are generated by `generate.py`

**Checkpoint**: Directory structure in place; fixture project initialized.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The `docker-bake.hcl` and `mk/kind.mk` skeleton are shared by US1 and US2; `values-test.yaml` testMode block is shared by US2 and US5. These must exist before user story phases begin.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T005 Write `docker-bake.hcl` defining one BuildKit target per service (`api-gateway`, `ws-server`, `scrape-orchestrator`, `proxy-manager`, `alert-engine`, `alert-dispatcher`, `spider-workers`, `pipeline`, `ml`, `ai-chat`, `frontend`). Each target: `context = "."`, `dockerfile = "services/<svc>/Dockerfile"` (or `frontend/Dockerfile`), `tags = ["localhost:5001/<svc>:dev"]`, `cache-from = ["type=local,src=.buildx-cache"]`, `cache-to = ["type=local,dest=.buildx-cache,mode=max"]`
- [X] T006 Create `mk/kind.mk` skeleton: define variables `CLUSTER_NAME ?= estategap`, `DOCKER_REGISTRY ?= localhost:5001`, `KIND_CONFIG ?= tests/kind/cluster.yaml`, `NAMESPACE ?= estategap-system`; add `.PHONY` declarations for all `kind-*` and `helm-*` targets; stub each target with `@echo "TODO: $@"` body (targets: `kind-up kind-down kind-build kind-load kind-deploy kind-seed kind-test kind-logs kind-shell kind-reset helm-lint helm-test helm-conformance helm-template helm-upgrade-test kind-prereqs`)
- [X] T007 Add `include mk/kind.mk` to the root `Makefile` (insert after the last existing variable definition, before the first `.PHONY` line)
- [X] T008 Write `helm/estategap/values-test.yaml` per data-model.md §4: set `cluster.environment: test`, `global.testMode: true`, `global.imageRegistry: localhost:5001`, disable prometheus/loki/tempo/keda (`enabled: false`), set all NATS/PostgreSQL/MinIO to minimal storage (1Gi/5Gi/1Gi), set Redis `architecture: standalone`, set `replicaCount: 1` and `hpa.enabled: false` for every service, add `testMode:` block with `enabled: true`, `nowOverride: ""`, `stripeWebhookSecret: whsec_test_fake`, `fixtureMinIOBucket: fixtures`, `fakeLLMProvider: true`, `scheduleOverride: "*/30 * * * * *"`

**Checkpoint**: Foundation ready — all user story phases can now proceed.

---

## Phase 3: User Story 1 — Developer Spins Up Full Local Platform (Priority: P1) 🎯 MVP

**Goal**: `make kind-reset` creates a 3-node kind cluster, builds images, deploys the Helm chart, and activates port forwards — all in under 5 minutes.

**Independent Test**: On a machine with Docker and kind installed, run `make kind-reset`. Expect all commands to exit 0, the cluster to have 3 nodes (`kubectl get nodes`), and `curl localhost:8080/healthz` to return 200.

- [X] T009 [US1] Write `tests/kind/cluster.yaml` per data-model.md §1: `kind: Cluster`, `apiVersion: kind.x-k8s.io/v1alpha4`, `name: estategap`, `kindest/node:v1.30.0`, control-plane with `ingress-ready=true` node label and 8 `extraPortMappings` (80→80, 443→443, 30080→8080, 30081→8081, 30090→9090, 30300→3001, 30003→3000, 30432→5432), 2 worker nodes, `containerdConfigPatches` for `localhost:5001` mirror
- [X] T010 [US1] Write `tests/kind/registry-configmap.yaml`: a `ConfigMap` named `local-registry-hosting` in namespace `kube-public` with `data.localRegistryHosting.v1` YAML pointing to `host: localhost:5001` and `help: https://kind.sigs.k8s.io/docs/user/local-registry/`
- [X] T011 [US1] Write `tests/kind/port-forward.sh`: `set -euo pipefail`; define array of forward specs `("8080:svc/api-gateway" "8081:svc/ws-server" "3000:svc/frontend" "3001:svc/grafana" "9090:svc/prometheus-operated" "5432:svc/estategap-postgresql-rw")`; start each with `kubectl port-forward -n estategap-system <spec> &` and append PID to `.kind-pids`; trap `SIGTERM SIGINT` to kill all PIDs
- [X] T012 [US1] Write `tests/kind/cleanup.sh`: `set -euo pipefail`; if `.kind-pids` exists, read each PID and `kill -9 $pid 2>/dev/null || true`; remove `.kind-pids`; then run `kind delete cluster --name estategap`
- [X] T013 [US1] Implement `kind-prereqs` target in `mk/kind.mk`: check for `kind`, `kubectl`, `helm`, `docker`, `docker buildx`; print install hint and exit 1 if any missing; also check `helm plugin list | grep unittest` and install if absent (`helm plugin install https://github.com/helm-unittest/helm-unittest`)
- [X] T014 [US1] Implement `kind-up` target in `mk/kind.mk`: (1) start local registry `docker run -d --restart=always -p 5001:5000 --name kind-registry registry:2` if not running; (2) `kind create cluster --config $(KIND_CONFIG) --name $(CLUSTER_NAME)`; (3) `docker network connect kind kind-registry 2>/dev/null || true`; (4) `kubectl apply -f tests/kind/registry-configmap.yaml`; (5) apply nginx ingress for kind: `kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml`; (6) `kubectl wait --namespace ingress-nginx --for=condition=ready pod --selector=app.kubernetes.io/component=controller --timeout=90s`
- [X] T015 [US1] Implement `kind-down` target in `mk/kind.mk`: call `bash tests/kind/cleanup.sh`; also stop the local registry `docker rm -f kind-registry 2>/dev/null || true`
- [X] T016 [US1] Implement `kind-build` target in `mk/kind.mk`: for each service in `GO_SERVICES PYTHON_SERVICES frontend`, compute `sha256sum` hash of `Dockerfile + source dir`, compare to `.make-cache/<svc>.digest`; if changed, run `docker buildx bake <target> --load -f docker-bake.hcl` and update digest; skip if unchanged. Use `$(MAKE) -j` or a shell loop with parallel detection.
- [X] T017 [US1] Implement `kind-load` target in `mk/kind.mk`: for each service image `localhost:5001/<svc>:dev`, run `docker push localhost:5001/<svc>:dev`. Images reach kind nodes via containerd mirror — no `kind load` needed.
- [X] T018 [US1] Implement `kind-deploy` target in `mk/kind.mk`: (1) `helm dependency update helm/estategap`; (2) `helm upgrade --install estategap helm/estategap -f helm/estategap/values.yaml -f helm/estategap/values-test.yaml --namespace $(NAMESPACE) --create-namespace --wait --timeout 5m`; (3) `bash tests/kind/port-forward.sh`
- [X] T019 [US1] Implement `kind-logs` target in `mk/kind.mk`: if `SERVICE` is set, `kubectl logs -f -n $(NAMESPACE) -l app.kubernetes.io/name=$(SERVICE) --prefix`; else if `stern` is available, `stern -n $(NAMESPACE) .`; else `kubectl logs -f -n $(NAMESPACE) --selector app.kubernetes.io/part-of=estategap --prefix --max-log-requests 20`
- [X] T020 [US1] Implement `kind-shell` target in `mk/kind.mk`: require `SERVICE` variable (error if empty); `kubectl exec -it -n $(NAMESPACE) deploy/$(SERVICE) -- /bin/sh`
- [X] T021 [US1] Implement `kind-reset` target in `mk/kind.mk`: chain `kind-down kind-up kind-build kind-load kind-deploy kind-seed` as sequential prerequisites (use `$(MAKE)` calls in recipe body to ensure proper error propagation)

**Checkpoint**: `make kind-reset` runs end-to-end; `kubectl get nodes` shows 3 nodes; port forwards active.

---

## Phase 4: User Story 2 — Helm Chart Passes All Validation Gates (Priority: P1)

**Goal**: `helm lint`, `helm template`, and `helm unittest` all exit 0 with zero warnings across all four values profiles. `values.schema.json` rejects invalid inputs.

**Independent Test**: Run `make helm-lint && make helm-test && make helm-conformance` in a clean checkout. All exit 0. Run `helm install estategap helm/estategap --set global.imageRegistry=123invalid --dry-run` and verify non-zero exit with schema error message.

- [X] T022 [US2] Modify `helm/estategap/templates/configmap.yaml`: add a conditional block `{{- if .Values.testMode.enabled }}` that appends these keys to the ConfigMap data: `ESTATEGAP_TEST_MODE: "true"`, `NOW_OVERRIDE: "{{ .Values.testMode.nowOverride }}"`, `FAKE_LLM_PROVIDER: "true"`, `TEST_SCHEDULE_OVERRIDE: "{{ .Values.testMode.scheduleOverride }}"`, `STRIPE_WEBHOOK_SECRET: "{{ .Values.testMode.stripeWebhookSecret }}"`, `FIXTURE_MINIO_BUCKET: "{{ .Values.testMode.fixtureMinIOBucket }}"`. Read the existing configmap.yaml first to find the correct insertion point.
- [X] T023 [US2] Write `helm/estategap/values.schema.json` per data-model.md §5: root object with `"additionalProperties": false`; define `$defs` for: `global` (imageRegistry string, imagePullSecrets array, testMode boolean), `cluster` (environment enum ["test","staging","production"], domain string, certIssuer string), `testMode` (enabled boolean, nowOverride string, stripeWebhookSecret string, fixtureMinIOBucket string, fakeLLMProvider boolean, scheduleOverride string), `observabilityComponent` (enabled boolean), `serviceSpec` (replicaCount integer minimum 1, image object with repository+tag strings, hpa object with enabled boolean + minReplicas + maxReplicas integers), `nats` (replicas integer, storage object), `postgresql` (instances integer, storage object), `redis` (architecture enum ["standalone","replication"]), `minio` (storage object), `services` (object with patternProperties `".*": {$ref: "#/$defs/serviceSpec"}`); reference all top-level keys from values.yaml
- [X] T024 [P] [US2] Write `helm/estategap/tests/api-gateway_test.yaml`: 4 test cases using `suite: api-gateway`, `templates: [templates/api-gateway-deployment.yaml]`: (1) default values render correct image tag from `global.imageRegistry`; (2) `replicaCount: 2` in set produces `spec.replicas: 2`; (3) `DATABASE_URL` env var is present in container env; (4) when `testMode.enabled: true`, `ESTATEGAP_TEST_MODE` appears in env (via configmap ref)
- [X] T025 [P] [US2] Write `helm/estategap/tests/autoscaling_test.yaml`: 3 test cases using HPA template file: (1) HPA resource is created when `hpa.enabled: true` with correct `minReplicas`/`maxReplicas`; (2) HPA resource count is 0 when `hpa.enabled: false`; (3) `maxReplicas` value flows from values into `spec.maxReplicas`
- [X] T026 [P] [US2] Write `helm/estategap/tests/ingress_test.yaml`: 3 test cases using ingress template: (1) host field contains `cluster.domain` value; (2) api-gateway path rule is present; (3) ws-server path rule is present
- [X] T027 [P] [US2] Write `helm/estategap/tests/network-policies_test.yaml`: 4 test cases using network-policies template: (1) api-gateway NetworkPolicy has ingress from `ingress-nginx` namespace selector; (2) PostgreSQL egress rule restricts to `estategap-data` namespace; (3) inter-service NetworkPolicy denies cross-namespace traffic by default; (4) NATS egress is allowed from all service namespaces
- [X] T028 [P] [US2] Write `helm/estategap/tests/secrets_test.yaml`: 3 test cases using sealed-secrets template: (1) resource kind is `SealedSecret` (not `Secret`); (2) expected keys (`encryptedData`) are present; (3) no values contain plaintext passwords/tokens (assert no match against pattern `password|secret|key` in `.spec.encryptedData` values being empty strings)
- [X] T029 [P] [US2] Write `helm/estategap/tests/postgres_test.yaml`: 3 test cases using postgresql-cluster template: (1) `spec.instances` matches `postgresql.instances` value; (2) `spec.storage.size` matches `postgresql.storage.size`; (3) `spec.postgresql.parameters` or bootstrap config references PostGIS extension
- [X] T030 [P] [US2] Write `helm/estategap/tests/nats_test.yaml`: 3 test cases: (1) NATS replica count from values flows to StatefulSet; (2) JetStream is enabled in NATS config; (3) storage size from values flows to PVC claim
- [X] T031 [P] [US2] Write `helm/estategap/tests/feature-flags_test.yaml`: 4 test cases: (1) kube-prometheus-stack resources have count 0 when `prometheus.enabled: false`; (2) loki resources have count 0 when `loki.enabled: false`; (3) KEDA ScaledObject has count 0 when `keda.enabled: false`; (4) ConfigMap data contains `ESTATEGAP_TEST_MODE: "true"` when `testMode.enabled: true`
- [X] T032 [US2] Implement `helm-lint` target in `mk/kind.mk`: run `helm lint helm/estategap -f helm/estategap/values.yaml` then repeat with each overlay (`values-staging.yaml`, `values-production.yaml`, `values-test.yaml`); fail fast on first non-zero exit
- [X] T033 [US2] Implement `helm-test` target in `mk/kind.mk`: run `helm unittest helm/estategap --file 'tests/*.yaml'`
- [X] T034 [US2] Implement `helm-template` target in `mk/kind.mk`: render all four profiles with `helm template estategap helm/estategap -f helm/estategap/values.yaml [-f <overlay>]` and pipe to `kubectl apply --dry-run=client -f -` for each (validates YAML structure)
- [X] T035 [US2] Implement `helm-conformance` target in `mk/kind.mk`: `helm template estategap helm/estategap -f helm/estategap/values.yaml -f helm/estategap/values-test.yaml | uv run --project tests/fixtures python tests/helm/conformance.py`

**Checkpoint**: `make helm-lint && make helm-test` both exit 0; 27 helm-unittest cases pass; schema rejects invalid values.

---

## Phase 5: User Story 3 — Helm Install/Upgrade/Rollback Verified (Priority: P2)

**Goal**: `tests/helm/install-test.sh` and `tests/helm/upgrade-test.sh` both exit 0, proving deploy, data persistence, and rollback work correctly.

**Independent Test**: Against a running kind cluster, run `bash tests/helm/install-test.sh`. Then run `bash tests/helm/upgrade-test.sh`. Both should exit 0. `kubectl get pods -A` should show no CrashLoopBackOff or ImagePullBackOff.

- [X] T036 [US3] Write `tests/helm/install-test.sh`: `set -euo pipefail`; (1) `helm upgrade --install estategap helm/estategap -f helm/estategap/values.yaml -f helm/estategap/values-test.yaml -n estategap-system --create-namespace --wait --timeout 5m`; (2) wait loops: `kubectl wait --for=condition=available deployment --all -n estategap-system --timeout=3m` (repeat for each namespace: estategap-system); (3) for each service endpoint `(api-gateway:8080 ws-server:8081)`, start `kubectl port-forward svc/$svc $port:$port -n estategap-system &` and `curl -sf http://localhost:$port/readyz` with retry (5 attempts, 5s sleep); (4) kill port-forwards; (5) capture all pod logs to `/tmp/estategap-install-test.log` on failure with `trap`
- [X] T037 [US3] Write `tests/helm/upgrade-test.sh`: `set -euo pipefail`; (1) package current chart as v0.1.0: `helm package helm/estategap -d dist/ --version 0.1.0`; (2) copy chart, bump `version: 0.2.0` in Chart.yaml of the copy, package as v0.2.0: `helm package /tmp/estategap-v2 -d dist/ --version 0.2.0`; (3) install v0.1.0: `helm install estategap dist/estategap-0.1.0.tgz -f values-test.yaml -n estategap-system --create-namespace --wait --timeout 5m`; (4) run seed: `uv run --project tests/fixtures python tests/fixtures/load.py`; (5) snapshot row count: `BEFORE=$(kubectl exec -n estategap-system estategap-postgresql-1 -- psql -U app -d estategap -tAc "SELECT COUNT(*) FROM listings")`; (6) upgrade: `helm upgrade estategap dist/estategap-0.2.0.tgz -f values-test.yaml -n estategap-system --wait --timeout 5m`; (7) verify count: `AFTER=$(kubectl exec ...)` and `[ "$BEFORE" -eq "$AFTER" ]`; (8) rollback: `helm rollback estategap 1 -n estategap-system --wait --timeout 3m`; (9) final health check: `curl -sf http://localhost:8080/healthz`; (10) cleanup: `helm uninstall estategap -n estategap-system 2>/dev/null || true`
- [X] T038 [US3] Write `tests/helm/schema-test.sh`: `set -euo pipefail`; run `helm install estategap-schema-test helm/estategap --dry-run --set global.imageRegistry=123` and expect non-zero exit (capture with `if helm install ...; then echo "FAIL: schema should have rejected invalid value"; exit 1; fi`); run same with `--set postgresql.instances=-1` (invalid minimum); print "Schema validation: PASSED"
- [X] T039 [US3] Implement `helm-upgrade-test` target in `mk/kind.mk`: `bash tests/helm/upgrade-test.sh`
- [X] T040 [US3] Implement `kind-test` target in `mk/kind.mk`: `bash tests/helm/install-test.sh && bash tests/helm/schema-test.sh`

**Checkpoint**: Install test exits 0 (all pods ready < 3 min); upgrade test exits 0 (no data loss, rollback succeeds).

---

## Phase 6: User Story 4 — Seed Data Populates Realistic Test State (Priority: P2)

**Goal**: `make kind-seed` inserts 5 users, 1,000 listings, 7 city zones, 10 alerts, ONNX models in MinIO, and sample conversations — all idempotently in under 30 seconds.

**Independent Test**: After `make kind-deploy`, run `make kind-seed`. Then `kubectl exec` into PostgreSQL and run `SELECT COUNT(*) FROM listings` — expect 1000. Check MinIO: `mc ls local/models/` shows 5 `.onnx` files.

- [X] T041 [P] [US4] Write `tests/fixtures/users.json`: 5 user objects (one per tier: free/basic/pro/global/api). Fields per data-model.md §2: `id` (deterministic UUID v5), `email` (`<tier>@test.estategap.com`), `password_hash` (bcrypt of `testpass123` — hardcoded hash string, not computed at load time), `subscription_tier`, `country`, `created_at`
- [X] T042 [P] [US4] Write `tests/fixtures/alerts.json`: 10 alert rule objects per data-model.md §2. Assign `user_id` to the pro-tier user UUID. Vary filters (country, max_price_eur, zone_id). All `active: true`.
- [X] T043 [P] [US4] Write `tests/fixtures/zones/es.json`: GeoJSON-like objects for Madrid (`POINT(-3.7038 40.4168)`, rough bounding polygon) and Barcelona. Include `id` (deterministic UUID), `name`, `country: ES`, `polygon_wkt` (WKT POLYGON approximating city boundary — simplified rectangle is acceptable for testing).
- [X] T044 [P] [US4] Write `tests/fixtures/zones/it.json`, `zones/fr.json`, `zones/pt.json`, `zones/gb.json` following the same schema as es.json — Rome and Milan for IT, Paris for FR, Lisbon for PT, London for GB
- [X] T045 [P] [US4] Write `tests/fixtures/listings/es.json`: array of 200 listing objects per data-model.md §2. Use realistic Spanish addresses, prices 150k–600k EUR, coordinates within Madrid/Barcelona bounding boxes, `portal: idealista`, `country: ES`, `status: active`, randomized bedroom/bathroom counts. UUIDs must be deterministic (e.g., UUID v5 from `es-listing-<index>`).
- [X] T046 [P] [US4] Write `tests/fixtures/listings/it.json`, `listings/fr.json`, `listings/pt.json`, `listings/gb.json` — 200 listings each for IT/FR/PT/GB using realistic local portal names (immobiliare.it, seloger.com, imovirtual.com, rightmove.co.uk) and country-appropriate prices/coordinates
- [X] T047 [P] [US4] Write `tests/fixtures/conversations/sample_01.json`: one AI chat conversation with `conversation_id`, `user_id` (free-tier user UUID), and 4 messages alternating `role: user` and `role: assistant` about property searching in Madrid
- [X] T048 [P] [US4] Write `tests/fixtures/html-samples/idealista/listing-page.html` and `detail-page.html`: minimal valid HTML mimicking Idealista's listing card structure (headline, price, area, address, img tags). Does not need to be pixel-perfect — enough for Scrapy CSS selectors to extract fields.
- [X] T049 [P] [US4] Write `tests/fixtures/html-samples/seloger/listing-page.html`, `html-samples/rightmove/listing-page.html`, `html-samples/rightmove/detail-page.html`, `html-samples/immobiliare/listing-page.html` — same approach as T048 for each portal's DOM structure
- [X] T050 [US4] Write `tests/fixtures/ml-models/generate.py`: using `sklearn.linear_model.LinearRegression` trained on 10 synthetic rows (features: `[area_m2, bedrooms, bathrooms]`, target: `price_eur`); export to ONNX via `skl2onnx.convert_sklearn`; save as `es.onnx`, `it.onnx`, `fr.onnx`, `pt.onnx`, `gb.onnx`. Validate each with `onnxruntime.InferenceSession` before saving. Include `if __name__ == "__main__": main()` guard.
- [ ] T051 [US4] Run `generate.py` to produce the 5 ONNX stub files: `cd tests/fixtures && uv run python ml-models/generate.py`. Commit the generated `.onnx` files (they are small stubs, <50KB each).
- [X] T052 [US4] Write `tests/fixtures/load.py`: async entry point using `asyncio.run(main())`; reads env vars `PG_DSN` (default: `postgresql://app:app@localhost:5432/estategap`), `REDIS_URL` (default: `redis://localhost:6379`), `MINIO_ENDPOINT`/`MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY` (defaults for kind); (1) open asyncpg connection pool; (2) `asyncpg.copy_records_to_table("users", ...)` with ON CONFLICT DO NOTHING fallback using `execute` loop; (3) bulk-insert listings via `copy_records_to_table("listings", ...)` from all 5 JSON files; (4) insert zones (PostGIS `ST_GeomFromText(wkt, 4326)`); (5) insert alerts; (6) insert conversations + messages; (7) upload ONNX files to MinIO bucket `models/` via boto3 `put_object`; (8) set Redis key `test:mode` to `"1"` via redis-py; (9) print timing summary and row counts; must complete in < 30s
- [X] T053 [US4] Implement `kind-seed` target in `mk/kind.mk`: wait for PostgreSQL ready (`kubectl wait --for=condition=ready pod -l cnpg.io/cluster=estategap-postgresql -n estategap-system --timeout=2m`); then `uv run --project tests/fixtures python tests/fixtures/load.py`

**Checkpoint**: `make kind-seed` exits 0 in < 30s; `SELECT COUNT(*) FROM listings` returns 1000; MinIO `models/` bucket has 5 ONNX files.

---

## Phase 7: User Story 5 — Test Mode Isolates External Dependencies (Priority: P3)

**Goal**: With `ESTATEGAP_TEST_MODE=true`, no real Stripe, LLM, or scraping calls are made. All external dependencies use in-process test doubles.

**Independent Test**: Deploy with `values-test.yaml` (test mode enabled). Trigger a scraping cycle via the scrape-orchestrator API. Verify that new listings in DB match fixture data (not live scrape). Check api-gateway logs for Stripe webhook — expect accepted with fake secret. Check ai-chat for LLM calls — expect no outbound HTTP to LLM endpoints.

- [X] T054 [US5] Write `tests/helm/conformance.py`: `set -euo pipefail` equivalent in Python (`import sys`); run `helm template estategap helm/estategap -f helm/estategap/values.yaml -f helm/estategap/values-test.yaml` via `subprocess.run`; parse multi-doc YAML with `yaml.safe_load_all()`; for each resource: (1) assert `metadata.namespace` is in `EXPECTED_NAMESPACES = {"estategap-system", "estategap-data", "estategap-scraping", "estategap-ml"}`; (2) assert required labels `app.kubernetes.io/name`, `app.kubernetes.io/instance`, `app.kubernetes.io/component`, `app.kubernetes.io/part-of` all present in `metadata.labels`; (3) for Deployment/StatefulSet kinds: iterate containers and assert `resources.requests` non-empty, `resources.limits` non-empty, `securityContext.runAsNonRoot == True`, `livenessProbe` defined, `readinessProbe` defined; (4) for any `image:` field found: assert not ending in `:latest`; accumulate failures as list; print each failure; `sys.exit(len(failures))` at end
- [X] T055 [US5] Add `FixtureSpider` stub to `services/spider-workers/`: create `services/spider-workers/src/spider_workers/spiders/fixture_spider.py`. Spider reads `FIXTURE_MINIO_BUCKET` env var, downloads listings JSON from MinIO using boto3, and publishes each listing to NATS `listings.raw.ES` (etc.) stream exactly as a real spider would — but using static fixture data. Activated when `ESTATEGAP_TEST_MODE=true` is detected in the service's config module.
- [X] T056 [US5] Add test-mode Stripe signature bypass to `services/api-gateway/`: in the Stripe webhook handler (locate via `Grep "stripe" services/api-gateway/`), add a check: if `ESTATEGAP_TEST_MODE == "true"`, accept any request with `Stripe-Signature` header matching `STRIPE_WEBHOOK_SECRET` env var value without cryptographic verification. Log `"[test-mode] Stripe webhook accepted with fake signature"`.
- [X] T057 [US5] Add `FakeLLMProvider` to `services/ai-chat/`: create `services/ai-chat/src/ai_chat/providers/fake_provider.py` implementing the existing LLM provider interface. Returns a hardcoded streaming response: `"This is a test response from FakeLLMProvider. Found 3 matching properties in your area."` Activated by checking `FAKE_LLM_PROVIDER=true` env var in the provider factory/selector (locate the provider factory via `Grep "LLMProvider\|litellm\|provider" services/ai-chat/`).
- [X] T058 [US5] Add `TEST_SCHEDULE_OVERRIDE` support to `services/scrape-orchestrator/`: locate the cron/schedule configuration (via `Grep "cron\|schedule\|interval" services/scrape-orchestrator/`); if `TEST_SCHEDULE_OVERRIDE` env var is set, use it as the scraping cron expression instead of the configured value. Log `"[test-mode] Using schedule override: <value>"` at startup.
- [X] T059 [US5] Add `NOW_OVERRIDE` support across all services: for Go services (`api-gateway`, `ws-server`, `alert-engine`, `alert-dispatcher`, `scrape-orchestrator`), add a `nowFunc() time.Time` helper in `libs/pkg/timeutil/now.go` that returns `time.Unix(ts, 0)` if `NOW_OVERRIDE` env var is set and parseable, else `time.Now()`. For Python services (`spider-workers`, `pipeline`, `ml`, `ai-chat`), add `libs/common/estategap_common/time_util.py` with `def now() -> datetime` doing the same. Existing `time.Now()` / `datetime.now()` calls in service handlers do NOT need to be replaced as part of this task — only the utility needs to exist and be importable.

**Checkpoint**: `ESTATEGAP_TEST_MODE=true` deployed; no outbound calls to Stripe/LLM/portals; all health checks pass.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Documentation, .gitignore cleanup, and final validation.

- [X] T060 [P] Write `tests/kind/README.md` (or update top-level README if present) linking to `quickstart.md` and listing minimum hardware requirements (4 CPU, 8 GB RAM, 40 GB disk)
- [X] T061 [P] Add `make kind-prereqs` documentation to Makefile comments: inline comment block above `kind-prereqs` target listing all required tools and versions
- [ ] T062 [P] Verify `helm lint` passes for `values-production.yaml`: run `helm lint helm/estategap -f helm/estategap/values.yaml -f helm/estategap/values-production.yaml` and fix any warnings. The production values file already exists; this task ensures it passes lint with the new schema.
- [ ] T063 Run final validation checklist from plan.md: execute each acceptance criterion command and confirm all exit 0; document results in a brief `specs/029-kind-helm-validation/validation-results.md`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — start immediately
- **Foundational (Phase 2)**: Depends on Phase 1 — **blocks all user story phases**
- **US1 — Kind Cluster (Phase 3)**: Depends on Phase 2; no dependency on US2/US3/US4/US5
- **US2 — Helm Validation (Phase 4)**: Depends on Phase 2 (specifically T008 for values-test.yaml); no dependency on US1
- **US3 — Install/Upgrade Tests (Phase 5)**: Depends on Phase 2 and US2 (T032–T034 for lint/template targets)
- **US4 — Seed Data (Phase 6)**: Depends on Phase 2; no dependency on US1/US2/US3
- **US5 — Test Mode (Phase 7)**: Depends on Phase 2 (T008) and US2 (T022 configmap modification)
- **Polish (Phase 8)**: Depends on all user story phases complete

### User Story Dependencies

- **US1 (P1)**: Starts after Phase 2 — fully independent
- **US2 (P1)**: Starts after Phase 2 — fully independent of US1
- **US3 (P2)**: Starts after US2 complete (needs working helm lint/template)
- **US4 (P2)**: Starts after Phase 2 — fully independent of US1/US2
- **US5 (P3)**: Starts after US2 T022 complete (needs testMode ConfigMap block)

### Within Each User Story

- US1: T009-T012 (config files) can be written in parallel → T013-T021 (Makefile targets) are sequential per dependency
- US2: T024-T031 (all 8 helm-unittest files) fully parallel → T032-T035 (Makefile targets) sequential
- US3: T036-T038 (shell scripts) parallel → T039-T040 (Makefile targets) after scripts exist
- US4: T041-T051 (fixture files + ONNX generation) parallel → T052 (load.py) after all fixtures → T053 (Makefile target) last
- US5: T054 (conformance.py) independent; T055-T059 (service stubs) parallel to each other

---

## Parallel Execution Examples

```bash
# Phase 2: Run foundational tasks in parallel
Task: "docker-bake.hcl — define all 11 BuildKit targets (T005)"
Task: "mk/kind.mk skeleton — stub all targets (T006)"
Task: "values-test.yaml — write test profile (T008)"
# Then: T007 (Makefile include) after T006 exists

# US1 Phase 3 — config files in parallel:
Task: "tests/kind/cluster.yaml (T009)"
Task: "tests/kind/registry-configmap.yaml (T010)"
Task: "tests/kind/port-forward.sh (T011)"
Task: "tests/kind/cleanup.sh (T012)"

# US2 Phase 4 — all helm-unittest files in parallel:
Task: "api-gateway_test.yaml (T024)"
Task: "autoscaling_test.yaml (T025)"
Task: "ingress_test.yaml (T026)"
Task: "network-policies_test.yaml (T027)"
Task: "secrets_test.yaml (T028)"
Task: "postgres_test.yaml (T029)"
Task: "nats_test.yaml (T030)"
Task: "feature-flags_test.yaml (T031)"

# US4 Phase 6 — all fixture files in parallel:
Task: "users.json (T041)"
Task: "alerts.json (T042)"
Task: "zones/es.json (T043)"
Task: "zones/it+fr+pt+gb.json (T044)"
Task: "listings/es.json (T045)"
Task: "listings/it+fr+pt+gb.json (T046)"
Task: "conversations/sample_01.json (T047)"
Task: "html-samples/ all portals (T048, T049)"

# US5 Phase 7 — service stubs in parallel:
Task: "FixtureSpider stub (T055)"
Task: "Stripe bypass in api-gateway (T056)"
Task: "FakeLLMProvider in ai-chat (T057)"
Task: "Schedule override in scrape-orchestrator (T058)"
Task: "NOW_OVERRIDE utility (T059)"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete **Phase 1**: Setup — create directories
2. Complete **Phase 2**: Foundational — docker-bake.hcl, mk/kind.mk skeleton, values-test.yaml
3. Complete **Phase 3 (US1)**: Kind cluster files + all Makefile targets
4. **STOP and VALIDATE**: `make kind-reset` completes end-to-end; `curl localhost:8080/healthz` returns 200
5. Demo: Full local cluster running

### Incremental Delivery

1. Setup + Foundational → Directory structure + build tooling ready
2. US1 → Local kind cluster operational (`make kind-reset` works)
3. US2 → Helm validation suite green (`helm lint`, `helm unittest`, `helm-conformance` all pass)
4. US3 + US4 → Install/upgrade tests pass + seed data loads
5. US5 → Test mode isolates all external services
6. Polish → Documentation and final validation

### Parallel Team Strategy

With 3 developers after Phase 2 completes:
- **Developer A**: US1 (Kind infrastructure) + US3 (install/upgrade tests)
- **Developer B**: US2 (Helm validation — schema + unittest suite)
- **Developer C**: US4 (Seed data fixtures) + US5 (Test mode stubs)

---

## Notes

- [P] tasks touch different files — safe to run in parallel
- Each user story is independently testable without requiring other stories to be complete
- Fixture UUIDs must be deterministic (use UUID v5 or hardcoded values) so load.py is idempotent
- `set -euo pipefail` required in all Bash scripts
- Python scripts must pass `ruff check` and `mypy --strict`
- All ONNX stubs must load successfully with `onnxruntime.InferenceSession` before committing
- Commit after each phase checkpoint to enable bisecting if issues arise
