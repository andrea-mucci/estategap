[pytest]
asyncio_mode = auto
markers =
    api: REST API end-to-end coverage
    ws: WebSocket end-to-end coverage
    concurrency: concurrent API and WebSocket scenarios
    slow: slower scenarios that intentionally wait on timeouts or bursts
testpaths =
    api
    websocket
    concurrency
